# Швидкий старт

Почніть працювати з ротацією акаунтів Google Antigravity за 5 хвилин!

## Крок 0: Налаштування OAuth (Опціонально, але рекомендовано)

Для використання повного OAuth 2.0 flow:

```bash
# Отримайте Client ID та Secret з Google Cloud Console
export GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
export GOOGLE_CLIENT_SECRET="your-client-secret"
```

**Як отримати:**
1. [Google Cloud Console](https://console.cloud.google.com/) → Ваш проект
2. "APIs & Services" → "Credentials" → "Create Credentials"
3. "OAuth 2.0 Client ID" → Desktop app
4. Redirect URI: `http://localhost:8888/callback`
5. Скопіюйте Client ID та Secret

Якщо не налаштовувати - можна використовувати ручне введення токенів.

## Крок 1: Встановлення розширення

Виберіть один з цих методів:

**Глобальне (рекомендовано для регулярного використання):**
```bash
# Скопіюйте в глобальну папку розширень
mkdir -p ~/.pi/agent/extensions
cp account-rotation.ts ~/.pi/agent/extensions/
```

**Локальне для проекту:**
```bash
# Створіть папку розширень проекту
mkdir -p .pi/extensions
cp account-rotation.ts .pi/extensions/
```

**Тимчасове (для тестування):**
```bash
# Завантажити лише для цієї сесії
pi -e ./account-rotation.ts
```

## Крок 2: Отримання облікових даних Google

### Варіант A: OAuth 2.0 Flow (Рекомендовано)

Якщо ви налаштували GOOGLE_CLIENT_ID та GOOGLE_CLIENT_SECRET:
- Розширення автоматично відкриє браузер
- Увійдіть в Google та дозвольте доступ
- Токени отримаються автоматично

### Варіант B: Ручне отримання токенів

Якщо OAuth не налаштовано, використовуйте OAuth 2.0 Playground:

1. Відвідайте https://developers.google.com/oauthplayground/
2. Натисніть ⚙️, поставте галочку "Use your own OAuth credentials"
3. Введіть ваш OAuth Client ID та Secret
4. Виберіть "Google Gemini API v1"
5. "Authorize APIs" → Увійдіть
6. "Exchange authorization code for tokens"
7. Скопіюйте access_token та refresh_token

## Крок 3: Налаштування акаунтів

Запустіть pi та виконайте налаштування:

```bash
pi
```

Потім у pi:

```
/rotationsetup
```

Слідуйте підказкам:

**З OAuth 2.0 (якщо налаштовано):**
```
Add Account (1) - Choose Method
> OAuth 2.0 Flow (Browser)

[Відкривається браузер → Вхід → Дозвіл доступу]

[УСПІХ] Tokens received!

Account Label (optional)
> Work Account

[УСПІХ] Account 1 added via OAuth!

Add Another? (Додати ще один?) Yes (Так)

Add Account (2) - Choose Method
> OAuth 2.0 Flow (Browser)

[Повторюється OAuth flow...]

Account Label
> Personal

[УСПІХ] Account 2 added via OAuth!

Add Another? No

[УСПІХ] Setup complete! 2 account(s) configured.
```

**Або з ручним введенням:**
```
Add Account (1) - Choose Method
> Manual Token Input

Add Account (1)
> Вставте OAuth JSON або токен доступу першого акаунту

[УСПІХ] Account 1 added!

Account Label (optional)
> Account A

Add Another? Yes

Add Account (2) - Choose Method  
> Manual Token Input

Add Account (2)
> Вставте OAuth JSON або токен доступу другого акаунту

[УСПІХ] Account 2 added!

Account Label
> Account B

Add Another? No

[УСПІХ] Setup complete! 2 account(s) configured.
```

## Крок 4: Перевірка роботи

Перевірте статус:

```
Запитайте pi: "Use the rotate_account tool to check rotation status"
              (Використай інструмент rotate_account для перевірки статусу ротації)
```

Pi має відповісти приблизно так:
```
rotate_account({ action: "status" })
→ 2 account(s) configured, currently using account 1, 0 rotation(s) performed
  (2 акаунт(и) налаштовано, зараз використовується акаунт 1, виконано 0 ротацій)
```

## Крок 5: Тестування автоматичної ротації

Тепер просто використовуйте pi звичайно! Коли досягнете обмеження швидкості:

```
Ви: [Ваш запит, який викликає обмеження]

[Pi виконує API виклик]
⚠️ Rate limit detected. Attempting to rotate account...
   (Виявлено обмеження. Спроба ротації акаунту...)
✓ Rotated to account 2/2 (rotation #1)
  (Переключено на акаунт 2/2 (ротація №1))

[Pi продовжує з новим акаунтом]
```

## Поширені проблеми

### "No accounts configured" (Акаунти не налаштовані)
→ Спершу виконайте `/rotationsetup`

### "Invalid credentials format" (Невірний формат облікових даних)
→ Переконайтеся, що ви вставляєте дійсний JSON або просту рядок токена

### "Failed to rotate account" (Не вдалося виконати ротацію акаунту)
→ Перевірте, що:
  - Токени все ще дійсні (не застарілі)
  - Провайдер google-antigravity існує
  - У вас увімкнено доступ до API

## Поради

- **Використовуйте 3+ акаунти** для кращої ротації
- **Тестуйте кожен токен** перед додаванням (зробіть тестовий API виклик)
- **Перевіряйте статус періодично** щоб бачити кількість ротацій
- **Перезапустіть /rotationsetup** якщо потрібно додати більше акаунтів
- **Зберігайте резервні токени** в безпечному місці

## Що далі?

- Прочитайте [README_UA.md](README_UA.md) для повної документації
- Перегляньте [SETUP_UA.md](SETUP_UA.md) для детальних інструкцій генерації токенів
- Запустіть [test-example.ts](test-example.ts) щоб зрозуміти внутрішню роботу

## Приклад сесії

Ось повний приклад від початку до кінця:

```bash
# 1. Запустіть pi з розширенням
pi -e ./account-rotation.ts

# 2. Налаштуйте акаунти
> /rotationsetup
[Додайте 3 акаунти через інтерактивні підказки]

# 3. Використовуйте звичайно
> "Згенеруй комплексний аналіз..."

[Працює нормально з акаунтом 1]

> "Тепер згенеруй ще один аналіз..."

[Досягнуто обмеження швидкості]
⚠️ Rate limit detected. Attempting to rotate account...
   (Виявлено обмеження. Спроба ротації акаунту...)
✓ Rotated to account 2/3 (rotation #1)
  (Переключено на акаунт 2/3 (ротація №1))

[Продовжує з акаунтом 2]

> "І ще один аналіз..."

[Знову досягнуто обмеження швидкості]
⚠️ Rate limit detected. Attempting to rotate account...
   (Виявлено обмеження. Спроба ротації акаунту...)
✓ Rotated to account 3/3 (rotation #2)
  (Переключено на акаунт 3/3 (ротація №2))

[Продовжує з акаунтом 3]

# 4. Перевіряйте статус будь-коли
> "Який статус ротації?"

[Pi використовує інструмент rotate_account]
→ 3 account(s) configured, currently using account 3, 2 rotation(s) performed
  (3 акаунт(и) налаштовано, зараз використовується акаунт 3, виконано 2 ротацій)
```

## Розширене: Скриптування

Ви також можете перевіряти статус програмно:

```typescript
// У вашій pi сесії, LLM може:
rotate_account({ action: "status" })  // Перевірити статус
rotate_account({ action: "rotate" })  // Вручну виконати ротацію
```

## Потрібна допомога?

- Перегляньте [README_UA.md](README_UA.md) для детальної документації
- Ознайомтеся з [SETUP_UA.md](SETUP_UA.md) для форматів облікових даних
- Створіть issue якщо знайдете баги
- Долучайтеся до покращень!

---

**Ось і все!** Тепер у вас є автоматична ротація акаунтів для Google Antigravity. 🎉

Коли ви досягнете обмежень швидкості, розширення автоматично перемкне акаунти та продовжить працювати!
